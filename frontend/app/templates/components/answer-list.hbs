{{#each answer as |item|}}
  <div class="card-block">
    <h5 class="card-question">
      {{item.value}}
    </h5>
    <button class="btn btn-secondary">
      →
    </button>
  </div>
{{else}}
  <div class="list-group-item">
    No items
  </div>
{{/each}}