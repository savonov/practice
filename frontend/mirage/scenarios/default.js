export default function(server) {
    server.createList('exercise', 7);
    server.createList('task',7);
    // server.loadFixtures('tasks');
    // server.create('task',{exercise_id:1});
    // server.loadFixtures('items');
    // server.create('item',{task_id:1})
}
